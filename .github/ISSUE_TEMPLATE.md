## Environment

- OS and version (e.g. Ubuntu 24.04, Debian 12):
- Install method (user service via `scripts/install-user` / local install via `scripts/install-local` / manual):
- `uv --version`:
- `npm --version`:
- `hermes --version` (if applicable):
- Hive OS git commit or tag (`git rev-parse --short HEAD`):

## Steps to reproduce

1.
2.
3.

## Expected behavior

<!-- What you expected to happen -->

## Actual behavior

<!-- What actually happened -->

## Logs

<!-- Paste relevant log output. For the service: `journalctl --user -u hive-os -n 100 --no-pager` -->

```
(paste logs here)
```
